import styled from "styled-components";

export const BtnPrimary = styled.button`
	height: 48px;
  font-weight: 500;
  font-size: 16px;
  line-height: 19px;
	background: ${(props) => props.theme.colors.primary600}; //primary-600
  color: #FFFFFF;
`
